import React from "react";
import Verify from "@/components/verify";

type Props = {};

const page = (props: Props) => {
  return <Verify type="forgot" description="Forgot Password" />;
};

export default page;
